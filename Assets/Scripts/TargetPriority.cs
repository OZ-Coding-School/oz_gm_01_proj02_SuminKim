public enum TargetPriority
{
    First,      // Enemy with the highest progress on the path
    Last,       // Enemy with the lowest progress
    Nearest,    // Closest to the tower
    Strongest   // Highest current HP
}
